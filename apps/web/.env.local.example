# Lab/portfolio demo (default on Vercel): embedded snapshots, no FastAPI required.
# Leave NEXT_PUBLIC_USE_API unset → IS_LAB=true

# Local full stack (optional):
# NEXT_PUBLIC_USE_API=true
NEXT_PUBLIC_API_URL=http://localhost:8000
